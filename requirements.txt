# t32 local harness. Auth for local dev is your Claude subscription via the Claude Code CLI —
# `claude setup-token` (or `claude login`). Prereq (not pip): `npm install -g @anthropic-ai/claude-code`.
claude-agent-sdk>=0.2
pyyaml>=6
rich>=13
